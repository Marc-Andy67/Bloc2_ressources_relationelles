<div class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-xl p-6 space-y-4">

    <div>
        <h2 class="text-lg font-semibold">
            {{ title }}
        </h2>

        {% if description is defined %}
            <p class="text-sm text-slate-500">
                {{ description }}
            </p>
        {% endif %}
    </div>

    {{ content|raw }}

</div>