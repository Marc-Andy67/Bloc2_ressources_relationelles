{% extends 'base.html.twig' %}

{% block title %}Ressources Mises de Côté - (RE)Sources Relationnelles{% endblock %}

{% block body %}
<div class="px-4 md:px-8 py-8 md:py-12 max-w-7xl mx-auto">
    <div class="flex justify-between items-end mb-8 border-b border-slate-200 pb-4">
        <div>
            <h1 class="text-3xl font-extrabold text-slate-800 tracking-tight flex items-center gap-3">
                {{ ux_icon('heroicons:bookmark', {class: 'w-8 h-8 text-amber-500'}) }}
                Mises de Côté
            </h1>
            <p class="text-slate-500 mt-2">Votre bibliothèque personnelle de ressources à lire plus tard.</p>
        </div>
        <a href="{{ path('app_ressource_index') }}" class="btn-primary text-sm bg-amber-500 hover:bg-amber-600 focus:ring-amber-500">Explorer d'autres contenus</a>
    </div>

    {% if ressources is empty %}
        <div class="glass-card flex flex-col items-center justify-center p-16 text-center border border-dashed border-slate-300">
            <div class="w-20 h-20 bg-slate-50 rounded-full flex items-center justify-center mb-4 text-slate-400">
                {{ ux_icon('heroicons:bookmark', {class: 'w-10 h-10'}) }}
            </div>
            <h2 class="text-xl font-bold text-slate-700 mb-2">Votre bibliothèque est vide</h2>
            <p class="text-slate-500 max-w-md mx-auto mb-6">
                Sauvegardez des ressources qui vous intéressent pour les consulter à votre rythme plus tard.
            </p>
            <a href="{{ path('app_ressource_index') }}" class="btn-primary bg-amber-500 hover:bg-amber-600 flex items-center gap-2">
                {{ ux_icon('heroicons:magnifying-glass', {class: 'w-5 h-5'}) }}
                Parcourir les ressources
            </a>
        </div>
    {% else %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {% for ressource in ressources %}
                <article class="glass-card overflow-hidden group hover:-translate-y-1 hover:shadow-xl transition-all duration-300 flex flex-col h-full border border-slate-200/60">
                    <div class="h-3 bg-gradient-to-r from-amber-400 to-amber-500 w-full"></div>
                    <div class="p-6 flex-grow flex flex-col">
                        <div class="flex justify-between items-start mb-4">
                            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-amber-100 text-amber-800">
                                {{ ux_icon('heroicons:tag', {class: 'w-3.5 h-3.5'}) }}
                                {{ ressource.category ? ressource.category.name : 'Général' }}
                            </span>
                            <span class="inline-flex items-center gap-1 text-slate-400 text-xs font-medium">
                                {{ ux_icon('heroicons:bookmark', {class: 'w-4 h-4 text-amber-500 fill-amber-500'}) }}
                            </span>
                        </div>
                        
                        <h2 class="text-xl font-bold text-slate-800 mb-3 group-hover:text-amber-600 transition-colors line-clamp-2">
                            {{ ressource.title }}
                        </h2>
                        
                        <p class="text-slate-600 text-sm mb-6 flex-grow line-clamp-3">
                            {{ ressource.content|striptags }}
                        </p>
                        
                        <div class="flex items-center justify-between mt-auto pt-4 border-t border-slate-100">
                            <div class="flex items-center gap-2 text-xs text-slate-500 font-medium">
                                {{ ux_icon('heroicons:user-circle', {class: 'w-5 h-5'}) }}
                                <span>{{ ressource.author.email|split('@')[0]|capitalize }}</span>
                            </div>
                            
                            <a href="{{ path('app_ressource_show', {'id': ressource.id}) }}" class="flex items-center justify-center w-8 h-8 rounded-full bg-slate-50 text-slate-400 hover:bg-amber-50 hover:text-amber-600 transition-colors" title="Voir la ressource">
                                {{ ux_icon('heroicons:arrow-right', {class: 'w-4 h-4'}) }}
                            </a>
                        </div>
                    </div>
                </article>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}
